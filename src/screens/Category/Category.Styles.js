import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        marginVertical:15,
        paddingHorizontal: 15,
        backgroundColor: "#fff",
      },
});

export { styles };
